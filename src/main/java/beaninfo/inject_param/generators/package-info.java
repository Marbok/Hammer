/**
 * Generators (Gens). That's like mixin in Ruby.
 * We have some methods and can include them in class.
 * This package includes generators, which help to convert strings to definite data
 */
package beaninfo.inject_param.generators;